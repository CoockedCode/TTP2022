<?php
    require_once("config.php");

    // session_start();
    // echo($_SESSION["status"]);


	// 	$conn = new mysqli($GLOBALS["server_host"], $GLOBALS["server_user_name"], $GLOBALS["server_password"], $GLOBALS["database"], $GLOBALS["db_port"]);
	// 	$conn->set_charset("utf8");

    //     $option = ["cost" => 12];
    //         $pwd_hash = password_hash(1, PASSWORD_BCRYPT, $option);

    // $stmt = $conn->prepare("INSERT INTO tootaja (id, eesnimi, perekonnanimi, user_name, email, password, palgal, in_edit) VALUES (NULL, 'Andrus', 'Juhe', '1', '1', ?,'1', '0')");

    // $stmt->bind_param("s", $pwd_hash);
    //         $stmt->execute();

    // 	$stmt->close();
	// 	$conn->close();



    // //	kas ikka on sess olemas?
    // session_start();
	// if ($_SESSION["status"] != 'true') {exit;}

    // // Kas on ikka GET päring?
	// if ($_SERVER['REQUEST_METHOD'] != 'GET') {exit;}